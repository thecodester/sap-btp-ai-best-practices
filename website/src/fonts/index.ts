import { Aleo } from "next/font/google";

export const aleo = Aleo({
	subsets: ["latin"],
	display: "swap"
});

export const aleoBold = Aleo({
	weight: "900",
	subsets: ["latin"],
	display: "swap"
});
